# GitHub Personal Access Token (with repo permissions)
# Get from: https://github.com/settings/tokens
# Required permissions: repo (full control of private repositories)
GITHUB_TOKEN=your_github_token_here

# Target GitHub repository (format: username/repository-name)
GITHUB_REPO=your-username/your-repo-name

# Git Configuration
GIT_EMAIL=your-email@example.com
GIT_NAME=Your Name

# GitHub branch to sync to (will be created if doesn't exist)
GITHUB_BRANCH=latex-docs

# Enable/disable automatic syncing (true/false)
ENABLE_SYNC=false

# How often to sync in seconds (default: 300 = 5 minutes)
SYNC_INTERVAL=300